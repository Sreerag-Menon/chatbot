# Web framework & server (essential)
fastapi
uvicorn[standard]

# Database ORM & driver
SQLAlchemy==2.0.31
psycopg2-binary>=2.9,<3    # PostgreSQL driver for production
pydantic[email]

# Auth & security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.6

# Core dependencies
requests
python-dotenv==1.0.1
websockets

# Minimal ML stack (CPU-only, memory optimized)
torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers --no-deps
transformers --no-deps
numpy
chromadb --no-deps

# LangChain (minimal set)
langchain-core
langchain-groq
langchain-chroma
langchain-text-splitters
langchain-huggingface

# Web scraping (lightweight)
beautifulsoup4
lxml
trafilatura

# Files & documents
pdfplumber

# APIs
groq

# Validation
email-validator